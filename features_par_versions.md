Java Features par version

🚀 Évolution des features Java (depuis Java 5)
Java 5 (2004)
Generics — révolution pour la sécurité de type (List<String>).
Annotations — fondation de Spring, JPA, tests, etc.

Java 6 (2006)
Scripting API (JSR‑223) — permet d’intégrer des langages (JavaScript, Groovy…).
Améliorations JVM/perf — pas de feature syntaxique majeure, mais une version très stable encore utilisée longtemps.

Java 7 (2011)
Try‑with‑resources — gestion automatique des ressources.
Diamond operator (<>) — simplifie l’usage des generics.

Java 8 (2014) — la version la plus transformative depuis Java 5
Lambdas & Streams — paradigme fonctionnel, pipelines, map/filter/reduce.
Optional — meilleure gestion de l’absence de valeur.

Java 9 (2017)
Modules (JPMS) — structuration des applications (moins utilisé mais fondamental côté JDK).
JShell — REPL pour tester du code rapidement.

Java 10 (2018)
var — inférence de type locale.
Améliorations GC — notamment G1 par défaut (impacte les perfs, pas la syntaxe).

Java 11 (2018) — LTS très répandue
Nouvelles méthodes String (isBlank, lines, repeat…).
HttpClient standard — API moderne pour HTTP/2.

Java 12 (2019)
Switch expressions (preview) — base du switch moderne.
JVM Constants API — utile pour les frameworks.

Java 13 (2019)
Text blocks (preview) — chaînes multilignes lisibles.
Refinements du switch — amélioration du nouveau modèle.

Java 14 (2020)
Records (preview) — classes immuables concises.
Pattern matching pour instanceof (preview) — simplifie les cast.

Java 15 (2020)
Text blocks (final) — adoptés massivement.
Sealed classes (preview) — contrôle de l’héritage.

Java 16 (2021)
Records (final) — désormais largement utilisés.
Pattern matching instanceof (final).

Java 17 (2021) — LTS très utilisée aujourd’hui
Sealed classes (final) — hiérarchies contrôlées.

Pattern matching pour switch (preview) — base du switch moderne.

Java 18 (2022)
Simple Web Server — pratique pour tests/POC.
UTF‑8 par défaut — enfin.

Java 19 (2022)
Virtual threads (preview) — révolution de la concurrence (Project Loom).
Structured concurrency (preview) — simplifie la gestion de tâches concurrentes.

Java 20 (2023)
Virtual threads (2e preview) — stabilisation.
Pattern matching switch (3e preview) — maturation.

Java 21 (2023) — LTS récente
Virtual threads (final) — probablement la plus grosse feature depuis Java 8.
Pattern matching switch (final) — switch moderne, exhaustif, puissant.

🧭 Résumé ultra‑condensé
Si tu dois retenir les 6 features les plus structurantes encore massivement utilisées :

Version	Feature
Java 5	Generics
Java 7	Try‑with‑resources
Java 8	Lambdas & Streams
Java 11	HttpClient
Java 16	Records
Java 21	Virtual
